# Codex Handoff — Steel-domain PatchCore (IndustrialVision-QC)

Generated by WorkBuddy on 2026-08-14. This document is a read-only handoff
snapshot. Verify every value below against local files before resuming. Do not
trust PASS markers blindly.

## 1. Git State

- branch: `feat/steel-domain-patchcore-v1.1`
- HEAD: `0d72347e29f050b8f56f7481022eafb64831b4f4`
- `git diff --check`: clean (no whitespace errors)
- working tree: many untracked files (source/tests/docs not yet committed). Key
  untracked paths:
  - `inference-service/scripts/{train,eval,failure_case,mlops_register}_steel_patchcore.py`
  - `inference-service/tests/test_steel_*.py`
  - `model-training/steel_patchcore/` (lifecycle.py, threshold_verify.py, tile.py, split.py, audit.py, etc.)
  - `docs/steel-domain-dataset-gate.md`
  - `.venv-steel/` (virtualenv, should NOT be committed)

## 2. Evaluation Progress (frozen, resumable)

- validation_normal: **590 / 590** COMPLETE (diagnostic only)
- test_normal: **400 / 591** (resumable from checkpoint)
- test_anomaly: **0 / 6666** (not started)

processed originals so far: 990 (590 + 400).

## 3. Checkpoint

- path: `model-training/datasets/severstal-steel/raw/steel_eval_ckpt.json`
- last updated: `2026-08-14 10:34:44 +0800`
- resumability: VERIFIED — test_normal 400 unique IDs, 0 duplicates, all belong
  to `test_normal` split; per-image fields `{score, pred}`. validation_normal 590
  unique. test_anomaly empty.
- structure: `{ "<split>": { "<image_id>": {"score": float, "pred": int, "pixel_auc"?: float, "aupro"?: float} } }`

## 4. Frozen Artifacts

- bank.npz: `inference-service/models/steel-patchcore/bank.npz`
- bank_meta.json: `inference-service/models/steel-patchcore/bank_meta.json`
- full bank SHA256: `291bb2903b6e9a3bc60ca4ae35380bd7cf312bc661366d10f87a75d3f0b8ebda`
- bank row_count=50000, feature_dim=1536, finite_only, seed=42,
  candidate_patches=33840128, source_train_normal=4721
- threshold.json: `model-training/datasets/severstal-steel/threshold.json`
  - threshold = `0.490039`
  - threshold_method = `max(train_normal original-image scores); original score = max over 7 tile scores`
  - bank_sha256 reference matches bank.npz (verified)
  - scored_originals = 4721, expected_originals = 4721
- train_normal_scores.json: `model-training/datasets/severstal-steel/train_normal_scores.json`
  (calibration evidence; 4721 scores)
- split_manifest.json: `model-training/datasets/severstal-steel/split_manifest.json`
  - full split SHA256: `64df9f817a995e64c7ee4d00e16962d94f500878fd1404d95227f26e7f913d07`
  - counts: train_normal=4721, validation_normal=590, test_normal=591, test_anomaly=6666

## 5. Frozen Experiment Protocol

- model = `steel-patchcore` `1.0.0`
- backbone: WideResNet-50-2, layer2 + layer3, feature dim 1536
- memory bank: 50,000 patches, source train_normal only, seed 42, reservoir sampling
- tiling: 256×1600 → seven 256×256 tiles, offsets 0,256,512,768,1024,1280,1344
- original-image score = max(7 tile scores)
- pixel overlap aggregation = existing frozen implementation
- threshold = 0.490039 (FROZEN)

## 6. Environment

- Python: 3.11.9
- torch: 2.11.0+cu128
- CUDA: 12.8 (available: True)
- GPU: NVIDIA GeForce RTX 5060
- venv: `.venv-steel` (project root; python at `.venv-steel/Scripts/python.exe`)

## 7. Commands

Preflight (safe to run before resume; read-only + temp-file only):

```
.venv-steel/Scripts/python.exe -c "import sys; sys.path.insert(0,'model-training'); from steel_patchcore.lifecycle import lifecycle_enter, find_stale_steel_processes; print(find_stale_steel_processes())"
```

Resume evaluation (single-instance + checkpoint resume + artifact hash verify):

```
.venv-steel/Scripts/python.exe -u inference-service/scripts/eval_steel_patchcore.py
```

Inspect checkpoint:

```
python -c "import json; d=json.load(open('model-training/datasets/severstal-steel/raw/steel_eval_ckpt.json')); print({k:len(v) for k,v in d.items()})"
```

Aggregate final metrics: `eval_steel_patchcore.py` writes metrics after all three
splits complete (see script tail for output file names).

## 8. Known WorkBuddy Sandbox Limitations

- TaskStop / session teardown may leave Python descendants alive; always
  re-verify process state after a session boundary.
- In-process stale termination (psutil children/kill and subprocess taskkill)
  can hang; perform stale cleanup via sandbox-external `taskkill /PID <pid> /T /F`.
- `steel_patchcore/lifecycle.py` implements the gate (single-instance lock +
  preflight + Job Object attempt), but Job Object returns False here; rely on
  lock + external cleanup + preflight.
- safe-delete hooks may block `unlink` of checkpoint/lock files (harmless).

## 9. Required Resume Protocol

1. external stale-process cleanup (precise: python + steel script + this workspace)
2. single-instance/preflight (lifecycle.py)
3. verify bank SHA256 / threshold.json / split SHA256 against the full values above
4. resume evaluation from checkpoint (never recompute completed images)

## 10. Remaining Tasks

1. complete test_normal (191 remaining) and test_anomaly (6666)
2. aggregate formal test metrics (Image AUROC, TP/TN/FP/FN, Precision/Recall/F1,
   Normal FPR, Anomaly Recall)
3. pixel metrics (Pixel AUROC, AUPRO) with stated aggregation semantics
4. four-group score distributions (train/val/test_normal/test_anomaly)
5. failure analysis
6. Optimization 1 verdict (STEEL_DOMAIN_VALIDATED or FAILED)
7. MLOps CANDIDATE registration (bank SHA, threshold artifact, split SHA, metrics)
8. tests (dataset/RLE/tiling/leakage/bank-immutability/threshold-binding/eval/fusion/MLOps)
9. git commit to feat/steel-domain-patchcore-v1.1 (no data/credentials/bank/checkpoints)

## 11. Known Limitations

- validation_normal is diagnostic only; never mix into formal test AUROC/confusion.
- domain_validated=true means "validated on the selected Severstal benchmark"
  only, not all steel mills / all production environments.
- No secrets are included here; Kaggle credentials are not in any tracked file.

Handoff audit status: READY (values above were read from local files at handoff time).
