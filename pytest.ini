[pytest]
addopts = -m "not integration and not gpu"
markers =
    integration: requires trained weights and live services (inference HTTP, PostgreSQL)
    gpu: requires a CUDA-capable GPU
testpaths = backend/tests inference-service/tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
